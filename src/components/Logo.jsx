import React from "react";
// import logo from "../assets/logo.webp"; // Adjust the path based on your folder structure

function Logo({ width = "100px" }) {
  return (
    <div>
      {/* <img src={logo} alt="Logo" style={{ width: width }} /> */}
      Logo
    </div>
  );
}

export default Logo;
